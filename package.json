{
  "name": "hyper-idp-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker Auth0 broker for first-party HttpOnly auth cookies.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/worker.js"
  },
  "scripts": {
    "check": "node --check src/worker.js && node --test src/worker.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.93.0"
  }
}
